# Diário de Bordo — 27 de Junho de 2026

## 📋 Resumo do Dia
Hoje foram implementadas melhorias críticas de infraestrutura, resiliência de rede e isolamento de dados de tanques com lotes inativos na produção. Adicionalmente, corrigimos problemas de poluição de módulos na suíte de testes e realizamos uma limpeza no banco de dados SQLite local.

---

## 🛠️ Detalhes das Alterações

### 1. Correção de Resolução de DNS Local (Pi-hole)
- **Host**: `alpine` (`192.168.1.7`)
- **Configuração**: Alterado o arquivo `/etc/pihole/pihole.toml` para definir o mapeamento estático local dos seguintes domínios:
  - `peixe` ➔ `192.168.1.99` (DietPi de produção)
  - `peixe.lan` ➔ `192.168.1.99`
  - `alpine.lan` ➔ `192.168.1.7` (Servidor Pi-hole / DNS secundário)
- **Impacto**: Garante que o resolvedor DNS local responda corretamente a requisições internas, evitando timeouts no Tailscale e loops infinitos no watchdog do sistema.

### 2. Validação e Monitoramento de Rede e IP Estático
- **Script**: Criado o utilitário [check_network_health.sh](file:///home/bruno/Documentos/4_HOMELAB/1_AQUICULTURA_DIETPI/generalsystemscraping/scripts/check_network_health.sh).
- **Funcionalidade**:
  - Confirma se o IP estático atual do host corresponde a `192.168.1.99`.
  - Testa a resolução DNS via `nslookup` contra o DNS Server local (`192.168.1.7`) para os domínios `peixe`, `peixe.lan` e `alpine.lan`.
  - Registra os resultados detalhados no log local em `logs/network_health.log`.
- **Agendamento**: Adicionado à tabela cron para rodar diariamente às **02:00**:
  ```cron
  0 2 * * * sh /home/bruno/generalsystemscraping/scripts/check_network_health.sh
  ```

### 3. Filtragem Dinâmica de Lotes Ativos
- **Escopo**: Alertas, relatórios, análises e consultas do bot de Telegram.
- **Implementação**:
  - Integração com o PostgreSQL para listar todas as estruturas com lotes ativos (`data_abate IS NULL`).
  - Atualização dos scripts de geração de relatórios de temperatura horários e de 7 dias (`hourly_report.py`, `bot_query_temp_7d.py`, etc.) para ignorar dinamicamente estruturas inativas como `"Tanque 1"` e `"N/A"`.
  - Alertas críticos e offline (`alert_check.py` e `offline_check.py`) atualizados para ignorar essas leituras para evitar alertas falsos.
- **Impacto**: Redução drástica de ruído em canais de telemetria, focando recursos apenas em estruturas produtivas vigentes.

### 4. Correções e Estabilidade na Suíte de Testes (TDD)
- **Poluição de Módulos (sys.modules)**: Adicionado o método `tearDownModule()` / `teardown_module()` nos testes (`test_agent_fallback.py`, `test_alert_check_filter.py`, `test_feed_prediction_agent.py` e `test_security_fixes.py`) para limpar o cache de `sys.modules`. Isso impede que mocks ou dependências importadas dinamicamente poluam o estado de outros testes.
- **Mock de Clima**: Adicionado suporte para simular a variável de cobertura de nuvens diária (`cloud_cover` / `d_var5` nos retornos do Open-Meteo SDK) dentro do arquivo de testes [test_weather.py](file:///home/bruno/Documentos/4_HOMELAB/1_AQUICULTURA_DIETPI/generalsystemscraping/tests/test_weather.py), evitando quebras causadas por índices de variáveis faltantes.

### 5. Limpeza de Dados Legados no SQLite Local
- **Ação**: Remoção manual/scripts de registros contendo dados de tanques nomeados `"N/A"` ou estruturas sem relação real com a produção.
- **Query Executada**:
  ```sql
  DELETE FROM leituras WHERE nome_estrutura = 'N/A' OR nome_estrutura IS NULL;
  ```
- **Impacto**: A base de dados SQLite local no DietPi agora está enxuta, otimizando o cache de inicialização e as rotinas de sincronização para o PostgreSQL.

---

## 📈 Status de Validação na Produção (peixe)
- Logs de produção inspecionados com sucesso via SSH (`ssh peixe`):
  - `scrape.log`: Operando corretamente, salvando dados do Tanque 2.
  - `alerts.log`: Filtrando e ignorando corretamente leituras do Tanque 1 e N/A (lotes inativos).
  - `network_health.log`: Validações de DNS e IP estático executadas com 100% de sucesso.
  - `migrate.log`: Sincronização incremental SQLite ➔ Postgres em lote concluída normalmente.
  - `watchdog.log`: Verificações executadas a cada 5 minutos sem incidentes.
