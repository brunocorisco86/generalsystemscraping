services:
  postgres:
    image: postgres:15-alpine
    container_name: piscicultura_postgres
    environment:
      POSTGRES_DB: ${PG_DBNAME:-piscicultura_history}
      POSTGRES_USER: ${PG_USER:-admin}
      POSTGRES_PASSWORD: ${PG_PASSWORD:-admin}
    ports:
      - "5432:5432"
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    restart: unless-stopped

  telegram_bot:
    build:
      context: .
      dockerfile: src/bots/Dockerfile
    container_name: telegram_bot
    env_file:
      - .env
    environment:
      - PG_HOST=postgres
    depends_on:
      - postgres
    restart: unless-stopped

# nodered:
#   build:
#     context: ./nodered
#     dockerfile: Dockerfile
#   container_name: piscicultura_nodered
#   ports:
#     - "1880:1880"
#   volumes:
#     - ./nodered:/data
#     - ${PROJECT_ROOT}:/app
#   environment:
#     - PROJECT_ROOT=/app
#     - TZ=America/Sao_Paulo
#   restart: always

# Volumes persistentes configurados via diretório local
# volumes:
#   postgres_data:
#     driver: local
